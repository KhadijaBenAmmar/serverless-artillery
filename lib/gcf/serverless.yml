service: gcf # NOTE: Don't put the word "google" in here

provider:
  name: google
  runtime: nodejs
  region: us-central1
  memorySize: 2048
  project: vikj-61740
  # the path to the credentials file needs to be absolute
  credentials: ~/.gcloud/vikj-61740-5bad43be67be.json

plugins:
  - serverless-google-cloudfunctions

# needs more granular excluding in production as only the serverless provider npm
# package should be excluded (and not the whole node_modules directory)
package:
  include:
    - serverless.yml
  exclude:
    - node_modules/**
    - .gitignore
    - .git/**
functions:
#  loadGeneratorCP:
#    handler: loadGeneratorCP
#    timeout: 300s
#    events:
#      - event:
#          eventType: providers/cloud.pubsub/eventTypes/topic.publish
#          resource: projects/vikj-61740/topics/loadgeneratortopic3
#    environment:
#            AWS_ACCESS_KEY_ID: AKIAJTDKZCIY2K6W5COA
#            AWS_SECRET_ACCESS_KEY: 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
#            AWS_REGION: us-east-1
  loadGenerator:
      handler: loadGenerator
      timeout: 300s
      events:
        - http: path
      environment:
              AWS_ACCESS_KEY_ID: AKIAJTDKZCIY2K6W5COA
              AWS_SECRET_ACCESS_KEY: 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
              AWS_REGION: us-east-1
